import { createStore } from 'vuex'

import appState from "@/store/modules/app";

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    app: appState
  }
})
